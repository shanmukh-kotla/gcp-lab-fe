service:
  name: fe-app
  port: 80
  targetPort: 8080
  type: LoadBalancer

container:
  port: 8080

image:
  repository: us-central1-docker.pkg.dev/devops-lab-project-485312/apps
  tag: "1.0"

probes:
  readiness:
    httpGet:
      path: /healthz
      port: 8080
  liveness:
    httpGet:
      path: /healthz
      port: 8080
